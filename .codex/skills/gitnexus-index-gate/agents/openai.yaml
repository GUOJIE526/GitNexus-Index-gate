interface:
  display_name: "GitNexus Index Gate"
  short_description: "Check GitNexus before every use"
  default_prompt: "Use $gitnexus-index-gate before calling GitNexus: run status, analyze automatically if stale or missing, verify again, then continue."
